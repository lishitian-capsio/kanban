{
  "id": "8d613",
  "title": "T6 agent 引用 Files(pi 内联 / CLI @ / 运行时输入带附件)",
  "prompt": "让 pi 和 CLI agent 都能引用 Files 库里的文件,统一引用、各取所需。依赖 T4(Files 库后端)。\n\n要做:\n1. 契约:把消息契约从 images[] 泛化为 attachments[],每条引用 Files 库的 id + 元数据(协调 src/session/ 的 SessionMessage = runtimeTaskChatMessageSchema,wire schema 是 source of truth,避免 transcript 与广播漂移)。\n2. pi 消费:图片走 store.getBytes → 内联 base64 content(保持 pi-agent-runtime.ts buildUserMessage 的 vision 行为);文档走 getPath 按路径引用。\n3. CLI 消费:走 store.getPath → 以 @路径 注入发给 PTY 的文本。因为 files 已 commit,worktree 内 .kanban/files/ 路径稳定可用。\n4. CLI 运行时输入支持附件:runtimeTaskSessionInputRequestSchema 目前纯文本(src/core/api-contract.ts),扩展为可带 attachments;runtime-api.ts 的 CLI 分支(约 line 360)与 terminal/session-manager.ts writeInput 对附件解析路径并注入。\n5. 重构 src/terminal/task-image-prompt.ts:从 mkdtemp/tmpdir 改为引用 Files 库路径,启动 prompt 与运行时输入共用同一套路径注入。\n6. 写测试。遵守 AGENTS.md。",
  "startInPlanMode": false,
  "autoReviewEnabled": true,
  "autoReviewMode": "commit",
  "owner": {
    "name": "Shitian",
    "email": "shitian@capsio.ai"
  },
  "baseRef": "main",
  "createdAt": 1781162347204,
  "updatedAt": 1781171115058,
  "column": "trash",
  "rank": "aSp",
  "dependsOn": []
}